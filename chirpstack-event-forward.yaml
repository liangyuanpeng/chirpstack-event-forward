general:
  http:
    port: 8080
config:
  - name: "test"
    chirpstack:
      account: ""
      password: ""
      url: ""
    integrations:
      mqtt:
        enabled: true
        topicTemplate: "{{ .customerName }}/application/{{ .appid }}/device/{{ .devEUI }}/{{ .event }}"
        url: ""
        clientId: test
        qos: 1
        username: ""
        password: ""